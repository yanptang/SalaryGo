# M8 验收记录

> 结论：通过（本地服务与数据一致性验收）

- 页面状态覆盖 M1–M7 核心记录；
- 持仓新增、编辑均通过账本校验层；
- 行情时间、过期和失败状态可见；
- 图表直接使用估值、评分和策略数值；
- 数据写入本地仓库，刷新页面后重新读取；
- 服务仅绑定 `127.0.0.1`，写入要求 CSRF token。

未进行用户未要求的浏览器截图式视觉 QA；HTTP 与应用服务测试已通过。

